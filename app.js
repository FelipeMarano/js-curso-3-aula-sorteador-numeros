function sortear () { // puxando os Ids para dentro do javascript
    let quantidade = document.getElementById ("quantidade").value; // para pegar o valor que vai ser digitado
    let de = document.getElementById ("de").value;
    let ate = document.getElementById ("ate").value;
    alert (`Quantidade ${quantidade}`); // alert apenas para testar se esta funcionando
    alert (`de ${de}`);
    alert (`ate ${ate}`);
}